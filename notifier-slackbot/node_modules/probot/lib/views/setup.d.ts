export declare function setupView({ name, description, version, createAppUrl, manifest, }: {
    name?: string;
    description?: string;
    version?: string;
    createAppUrl: string;
    manifest: string;
}): string;
