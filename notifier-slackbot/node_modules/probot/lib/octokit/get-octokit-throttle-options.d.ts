import { type RedisOptions } from "ioredis";
import type { Logger } from "pino";
type Options = {
    log: Logger;
    redisConfig?: RedisOptions | string;
};
export declare function getOctokitThrottleOptions(options: Options): import("@octokit/plugin-throttling/dist-types/types").ThrottlingOptionsBase & import("@octokit/plugin-throttling/dist-types/types").SecondaryLimitHandler;
export {};
